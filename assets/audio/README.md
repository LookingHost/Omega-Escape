# Audio

```
audio/
├── music/   # Future: licensed or composed tracks (currently procedural via Web Audio)
├── sfx/     # Future: recorded foley if you move off procedural
└── voices/  # Future: voice lines per character (NEX, ZARA, VOID, RIFT, LUNA, ATLAS, NULL)
```

All current audio is synthesized live in `src/game/audio.ts` — zero audio files.
Keep this folder for when you add real assets; do not commit empty placeholder wavs.
