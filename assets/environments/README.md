# Environments

District and set-piece references.

```
environments/
├── city/       # Corporate District, Neon Downtown, Sky Bridge — parallax layers
├── training/   # Hologram simulation, wireframe, AI instructor
└── boss/       # Omega Tower, NULL arena, mech bay
```

Backgrounds are currently procedural in `engine.ts` (`drawParallax`, `drawCityLife`, `drawSky`).
Add concept art, MatCap references, or tile atlases here when you move from procedural to authored art.
